{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["node", "react", "react-dom"],
    // Built types, not core sources: resolving through the source alias makes
    // the dts generator emit declarations next to `packages/core/src`.
    "paths": {
      "@slash-editor/core": ["../core/dist/index.d.mts"]
    }
  },
  "include": ["src"]
}
